---
import { native_highlighter_sync } from "../../zig_lexer";
const { thpcode, href } = Astro.props;

const { html: native_html } = native_highlighter_sync(thpcode);
---

<a
	href={href}
	class="inline-block w-full py-2 font-mono whitespace-pre"
	set:html={native_html}
/>
