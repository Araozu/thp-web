# Tuples

Uses `#()` just to avoid confusion with function
calls (`()`).

## Definition

```thp
val person = #("John", "Doe", 32)

val #(name, surname, age) = person
```


## Signature

```thp
#(String, String, Int)
```



